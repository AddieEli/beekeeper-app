
 <!DOCTYPE html>
<html>
 

 
<!-- >>>>>>>>>>>>>>>> Breadcumb Area Start <<<<<<<<<<<<<<<< -->
    <section class="breadcumb_area background-overlay" style="background-image: url(https://media.npr.org/assets/img/2013/05/13/honeycomb-2_wide-49ae2c6ec87d4b4092881baf81e2de967926cba5.jpg?s=1400);">
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <div class="breadcumb_section">
                        <!-- Breadcumb Page Title -->
                        <div class="page_title">
                            <h3> <%= @hive.hive_name %></h3>
                        </div>
                        <!-- Breadcumb Page Pagination -->
                        <ol class="breadcrumb">
                            <li><a href="index.html">Home</a></li>
                            <li class="active">something</li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- >>>>>>>>>>>>>>>> Breadcumb Area End <<<<<<<<<<<<<<<< -->

    <section class="section_padding_100">
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <h3> Hive Information: </h3>
                    <h4>Breed: <%= @hive.bee_breed %></h4>
                      <h4>City: <%= @hive.hive_city %></h4>
                      <h4>State: <%= @hive.hive_state%></h4>
                </div>
            </div>
        </div>
    </section>

    <section clas="section_padding_100"> 
      <div class="container">
        <div class ="row">
           <div class="col-x-12">
           <head>Upcoming Weather:</head>
      
          </div>  
        </div>  
      </div>
    </section>


<%= hidden_field_tag :hive_id, @hive_id %>



<h4>Current Temprature: <%= @temperature %>°<%= @temp_unit %></h4>

<h3>5 Day Forecast</h3>
  <% @forecasts.each do |forecast| %>
      <%= forecast["day"] %>, <%= forecast["date"] %>
      <ul>
        <li>Weather: <%= forecast["text"] %></li>
        <li>High: <%= forecast["high"] %></li>
        <li>Low: <%= forecast["low"] %></li>
      </ul>
  <% end %>


  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title>Circles</title>
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 80%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 80%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body>
  <h3>Foraging Distance</h3>
    <div id="map"></div>
    <script>

      var citymap = {
        <%= @hive.hive_city %>:{
          center: {lat: <%= @hive.latitude %>, 
                  lng: <%= @hive.longitude %>
          }
        }
      };

      function initMap() {
        // Create the map.
        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 12,
          center: {lat: <%= @hive.latitude %>, lng: <%= @hive.longitude %> },
          mapTypeId: 'terrain'
        });

        // Construct the circle for each value in citymap.
        // Note: We scale the area of the circle based on the population.
        for (var hive_city in citymap) {
          // Add the circle for this city to the map.
          var cityCircle = new google.maps.Circle({
            strokeColor: '#FF0000',
            strokeOpacity: 0.8,
            strokeWeight: 2,
            fillColor: '#FF0000',
            fillOpacity: 0.35,
            map: map,
            center: citymap[hive_city].center,
            radius: 4828.03
          });
        }
      }
    </script>
    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAOc24sApllxhbn4SF_ntEsMApDN26raEM&callback=initMap">
    </script>
  </body>





<%= link_to "Add a Log", "/hives/#{@hive.id}/logs/new" , class: "btn btn-primary"%>
  <br>
<h2> Most Recent Logs:</h2>

  <% @log.each do |log| %>
      <h4>Most Recent Update: <%= log.updated_at %></h4>
      <h6>Field Notes: <%= log.notes %></h6>
      <h6>Sightings: <%= log.queen%></h6> <h6> <%= log.larva %></h6> <h6> <%= log.comb_build %></h6>
      <h6>Overall Status <%= log.hive_general_status %></h6>
      <%= link_to "Delete Log", "/logs/#{ log.id }", method: :delete %>
      <br>
  <% end  %>

<%= link_to "Delete Hive", "/hives/#{ @hive.id }", method: :delete %>
  

 